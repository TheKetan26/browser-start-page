export default function Bookmarks(props) {
    var bookmarks = props.bookmarks

    bookmarks.map(bookmark => {
        return (
            <section>
                bookmark
            </section>
        )
    })

    return (
        <section>

        </section>
    )
}